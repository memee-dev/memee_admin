import 'package:flutter/material.dart';

class AdminsWidget extends StatelessWidget {
  const AdminsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AdminsWidget'),
    );
  }
}
