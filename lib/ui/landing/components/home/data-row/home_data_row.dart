import 'package:flutter/material.dart';

DataRow onGoingDataRow(
  BuildContext context,
) {
  return const DataRow(
    cells: [
      DataCell(Text('')),
       DataCell(Text('')),
      DataCell(Text('')),
    ],
  );
}
